--- a/src/compositor/meta-window-actor.c.orig	2022-01-05 21:50:42.877380641 -0500
+++ b/src/compositor/meta-window-actor.c	2022-01-05 21:50:25.930253189 -0500
@@ -670,7 +670,7 @@ on_visible_changed (MetaWindowActor *sel
   if (!priv->blur_actor)
     return;
 
-  if (priv->visible)
+  if (priv->visible && !priv->unminimize_in_progress)
     clutter_actor_show(priv->blur_actor);
   else
     clutter_actor_hide(priv->blur_actor);
@@ -947,7 +947,7 @@ meta_window_actor_show_blur (MetaWindowA
   clutter_actor_add_effect_with_name (priv->blur_actor,
                                       "ShellBlurEffect",
                                       CLUTTER_EFFECT(priv->blur_effect));
-    if (priv->visible)
+    if (priv->visible && !priv->unminimize_in_progress)
     clutter_actor_show(priv->blur_actor);
 }
 
@@ -1407,7 +1407,7 @@ meta_window_actor_sync_visibility (MetaW
 
   if (CLUTTER_ACTOR_IS_VISIBLE (self) != priv->visible)
     {
-      if (priv->visible)
+      if (priv->visible && !priv->unminimize_in_progress)
         clutter_actor_show (CLUTTER_ACTOR (self));
       else
         clutter_actor_hide (CLUTTER_ACTOR (self));
